# secrets
.env
.env.*
!.env.example

# obsidian per-user state
Weekender Build/.obsidian/workspace*.json
Weekender Build/.obsidian/cache
Weekender Build/.trash/

# node / build
node_modules/
dist/
.vercel/

# large media (brag renders) – share via drive/release instead
docs/launch/*.mp4

# os
.DS_Store
Thumbs.db

# skills CLI duplicate copies (canonical copies live in .claude/skills)
.agents/

# supabase local
supabase/.branches
supabase/.temp

# content import (scripts/import_content.py): generated SQL + Python cache
*.import.sql
__pycache__/
